filter:
    excluded_paths:
        - "tests/"

build:
    environment:
            php:
                version: 7.2
    tests:
        override:
            -
                command: vendor/bin/phpunit --coverage-clover=coverage
                coverage:
                    file: coverage
                    format: php-clover
